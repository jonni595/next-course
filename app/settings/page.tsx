import { roboto } from "@/app/fonts";

export const metadata = {
  title: "Settings",
  description: "Settings page",
};

export default function Settings() {
  return <div className={roboto.className}> Settings page</div>;
}
