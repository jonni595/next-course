import { roboto } from "@/app/fonts";

export const metadata = {
  title: "Profile",
  description: "Profile page",
};

export default function Profile() {
  return <div className={roboto.className}> Profile page</div>;
}
